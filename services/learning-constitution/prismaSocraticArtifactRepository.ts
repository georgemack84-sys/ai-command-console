import { prisma } from "../../src/server/db/prisma";
import type { SocraticArtifactRecord, SocraticArtifactStore } from "../../types/learning-constitution/socraticMentor";
import { canonicalizeAuditValue } from "./auditIntegrityHash";
type Row = { artifactId: string; workspaceId: string; artifactType: SocraticArtifactRecord["artifactType"]; subjectId: string; payload: unknown; createdAt: Date };
type Client = { noesisSocraticArtifact: { create(args: object): Promise<Row>; findUnique(args: object): Promise<Row | null>; findMany(args: object): Promise<Row[]> } };
const client = prisma as unknown as Client; const record = (row: Row): SocraticArtifactRecord => ({ artifactId: row.artifactId, artifactType: row.artifactType, subjectId: row.subjectId, payload: row.payload, createdAt: row.createdAt.toISOString() });
export class PrismaSocraticArtifactRepository implements SocraticArtifactStore { constructor(private readonly workspaceId: string, private readonly db: Client = client) {} async append(artifact: SocraticArtifactRecord): Promise<SocraticArtifactRecord> { const existing = await this.db.noesisSocraticArtifact.findUnique({ where: { artifactId: artifact.artifactId } }); if (existing) { if (existing.workspaceId !== this.workspaceId || existing.artifactType !== artifact.artifactType || existing.subjectId !== artifact.subjectId || canonicalizeAuditValue(existing.payload) !== canonicalizeAuditValue(artifact.payload)) throw new Error("Socratic artifact id collision"); return record(existing); } await this.db.noesisSocraticArtifact.create({ data: { ...artifact, workspaceId: this.workspaceId, payload: artifact.payload as object, createdAt: new Date(artifact.createdAt) } }); return artifact; } async listArtifacts(subjectId: string): Promise<readonly SocraticArtifactRecord[]> { return (await this.db.noesisSocraticArtifact.findMany({ where: { workspaceId: this.workspaceId, subjectId }, orderBy: { createdAt: "asc" } })).map(record); } async listWorkspaceArtifacts(): Promise<readonly SocraticArtifactRecord[]> { return (await this.db.noesisSocraticArtifact.findMany({ where: { workspaceId: this.workspaceId }, orderBy: { createdAt: "asc" } })).map(record); } }

import { prisma } from "../../src/server/db/prisma"; import { canonicalizeAuditValue } from "./auditIntegrityHash"; import type { DecisionArtifactRecord, DecisionArtifactStore } from "../../types/learning-constitution/decisionLearning";
type Row = Readonly<{ artifactId: string; workspaceId: string; artifactType: DecisionArtifactRecord["artifactType"]; subjectId: string; payload: unknown; createdAt: Date }>; type Client = Readonly<{ noesisDecisionArtifact: Readonly<{ findUnique(args: object): Promise<Row | null>; findMany(args: object): Promise<Row[]>; create(args: object): Promise<Row> }> }>; const client = prisma as unknown as Client;
export class PrismaDecisionArtifactRepository implements DecisionArtifactStore { constructor(private readonly workspaceId: string, private readonly db: Client = client) {} async append(artifact: DecisionArtifactRecord): Promise<DecisionArtifactRecord> { const existing = await this.db.noesisDecisionArtifact.findUnique({ where: { artifactId: artifact.artifactId } }); if (existing) { if (existing.workspaceId !== this.workspaceId || canonicalizeAuditValue(existing.payload) !== canonicalizeAuditValue(artifact.payload)) throw new Error("decision artifact id collision"); return { artifactId: existing.artifactId, artifactType: existing.artifactType, subjectId: existing.subjectId, payload: existing.payload, createdAt: existing.createdAt.toISOString() }; } await this.db.noesisDecisionArtifact.create({ data: { artifactId: artifact.artifactId, workspaceId: this.workspaceId, artifactType: artifact.artifactType, subjectId: artifact.subjectId, payload: artifact.payload as object, createdAt: new Date(artifact.createdAt) } }); return artifact; } async listArtifacts(subjectId: string): Promise<readonly DecisionArtifactRecord[]> { return (await this.db.noesisDecisionArtifact.findMany({ where: { workspaceId: this.workspaceId, subjectId }, orderBy: { createdAt: "asc" } })).map((row) => ({ artifactId: row.artifactId, artifactType: row.artifactType, subjectId: row.subjectId, payload: row.payload, createdAt: row.createdAt.toISOString() })); } }

import { prisma } from "../../src/server/db/prisma";
import { canonicalizeAuditValue } from "./auditIntegrityHash";
import type { AutonomousPracticeArtifactRecord, AutonomousPracticeArtifactStore } from "../../types/learning-constitution/autonomousPractice";

type Row = { artifactId: string; workspaceId: string; artifactType: AutonomousPracticeArtifactRecord["artifactType"]; subjectId: string; payload: unknown; createdAt: Date };
type Client = { noesisAutonomousPracticeArtifact: { create(args: object): Promise<Row>; findUnique(args: object): Promise<Row | null>; findMany(args: object): Promise<Row[]> } };
const client = prisma as unknown as Client;
const record = (row: Row): AutonomousPracticeArtifactRecord => ({ artifactId: row.artifactId, artifactType: row.artifactType, subjectId: row.subjectId, payload: row.payload, createdAt: row.createdAt.toISOString() });
export class PrismaAutonomousPracticeArtifactRepository implements AutonomousPracticeArtifactStore { constructor(private readonly workspaceId: string, private readonly db: Client = client) {} async append(artifact: AutonomousPracticeArtifactRecord): Promise<AutonomousPracticeArtifactRecord> { const existing = await this.db.noesisAutonomousPracticeArtifact.findUnique({ where: { artifactId: artifact.artifactId } }); if (existing) { if (existing.workspaceId !== this.workspaceId || existing.artifactType !== artifact.artifactType || canonicalizeAuditValue(existing.payload) !== canonicalizeAuditValue(artifact.payload)) throw new Error("autonomous practice artifact id collision"); return record(existing); } await this.db.noesisAutonomousPracticeArtifact.create({ data: { ...artifact, workspaceId: this.workspaceId, payload: artifact.payload as object, createdAt: new Date(artifact.createdAt) } }); return artifact; } async listArtifacts(subjectId: string) { return (await this.db.noesisAutonomousPracticeArtifact.findMany({ where: { workspaceId: this.workspaceId, subjectId }, orderBy: { createdAt: "asc" } })).map(record); } async listWorkspaceArtifacts() { return (await this.db.noesisAutonomousPracticeArtifact.findMany({ where: { workspaceId: this.workspaceId }, orderBy: { createdAt: "asc" } })).map(record); } }

import type { PreferenceCandidate, PreferenceCorrectionRepair, PreferenceTeachBackPlan } from "../../types/learning-constitution/preferenceLearning";
import type { CorrectionSignal } from "../../types/learning-constitution/correctionLearning";

/** Bridges Phase 11 and 12 without allowing either evidence type to mutate a preference automatically. */
export class PreferenceIntegrationService {
  planTeachBack(preference: PreferenceCandidate): PreferenceTeachBackPlan { const required = preference.strength === "STRONG" || preference.strength === "EXPLICIT" || preference.strength === "MANDATORY"; return { preferenceId: preference.preferenceId, requirement: required ? "REQUIRED" : "NOT_REQUIRED", lesson: `${preference.polarity}: ${preference.preferredOption}`, scope: preference.scope, uncertainty: "This preference does not apply outside its recorded scope or against a direct instruction.", persistenceEffect: "NONE", authorityEffect: "UNCHANGED", executionPermissionGranted: false }; }
  planCorrectionRepair(preference: PreferenceCandidate, correction: CorrectionSignal): PreferenceCorrectionRepair | null { if (!correction.targetCandidateIds.includes(preference.preferenceId)) return null; const text = correction.sourceText.toLowerCase(); if (/only.*(?:project|here|this)|not.*everything|not.*global/.test(text)) return { correctionId: correction.correctionId, preferenceId: preference.preferenceId, action: "NARROW_SCOPE", reason: "Correction indicates a scope overgeneralization; human lifecycle review must narrow the preference.", persistenceEffect: "NONE", authorityEffect: "UNCHANGED", executionPermissionGranted: false }; return { correctionId: correction.correctionId, preferenceId: preference.preferenceId, action: "REQUEST_CLARIFICATION", reason: "Correction targets a preference but does not establish a safe replacement scope.", persistenceEffect: "NONE", authorityEffect: "UNCHANGED", executionPermissionGranted: false }; }
}

import { getTrustProgramQualificationBundle, replayTrustProgramQualification, runTrustProgramQualification, validateTrustProgramQualification } from "@/services/trust-program-qualification";
import { getSessionUser } from "@/src/lib/auth";
import { AppError } from "@/src/server/api/errors";
import { requireWorkspaceMember } from "@/src/server/auth/permissions";
import type { TrustProgramQualificationInput, TrustProgramQualificationResult } from "@/types/trust-program-qualification";

export async function requireTrustProgramQualificationUser() { const user = await getSessionUser(); if (!user) throw new AppError(401, "unauthorized", "Authentication required."); await requireWorkspaceMember({ userId: user.id, userRole: user.role, workspaceId: user.workspaceId }); return user; }
async function readBody(request: Request): Promise<Record<string, unknown>> { return await request.json().catch(() => ({})); }
function inputFromBody(body: Record<string, unknown>): TrustProgramQualificationInput { return body as TrustProgramQualificationInput; }
function resultFromBody(body: Record<string, unknown>): TrustProgramQualificationResult { return (body.result as TrustProgramQualificationResult | undefined) ?? runTrustProgramQualification(inputFromBody(body)); }
export function contractResponse() { return getTrustProgramQualificationBundle(); }
export async function validateRequest(request: Request) { return validateTrustProgramQualification(resultFromBody(await readBody(request))); }
export async function scopeRequest(request?: Request) { const result = request ? resultFromBody(await readBody(request)) : runTrustProgramQualification(); return { constitutional_compliance: result.constitutional_compliance, architecture_completeness: result.architecture_completeness, deterministic_decision_production: result.deterministic_decision_production, trust_domain_resolution: result.trust_domain_resolution, evidence_integrity: result.evidence_integrity, confidence_modeling: result.confidence_modeling, risk_governance: result.risk_governance, alignment_verification: result.alignment_verification, compliance_verification: result.compliance_verification, safety_qualification: result.safety_qualification, explainability: result.explainability, human_oversight: result.human_oversight, continuous_monitoring: result.continuous_monitoring, drift_detection: result.drift_detection, recovery_revocation: result.recovery_revocation, certification_governance: result.certification_governance, ecosystem_federation: result.ecosystem_federation, deterministic_replay: result.deterministic_replay, evidence_completeness: result.evidence_completeness }; }
export async function ledgerRequest(request?: Request) { const result = request ? resultFromBody(await readBody(request)) : runTrustProgramQualification(); return { evidence_ledger: result.evidence_ledger }; }
export async function reportRequest(request?: Request) { const result = request ? resultFromBody(await readBody(request)) : runTrustProgramQualification(); return { report: result.report }; }
export async function decisionRequest(request?: Request) { const result = request ? resultFromBody(await readBody(request)) : runTrustProgramQualification(); return { decision: result.decision }; }
export async function replayRequest(request?: Request) { const result = request ? resultFromBody(await readBody(request)) : runTrustProgramQualification(); return { replayable: replayTrustProgramQualification(result), replay_hash: result.replay_hash, integrity_hash: result.integrity_hash }; }
export async function readinessRequest(request?: Request) { const result = request ? resultFromBody(await readBody(request)) : runTrustProgramQualification(); return { operational_readiness: result.operational_readiness }; }
export async function consumersRequest(request?: Request) { const result = request ? resultFromBody(await readBody(request)) : runTrustProgramQualification(); return { consumer_readiness: result.consumer_readiness }; }
export async function maturityRequest(request?: Request) { const result = request ? resultFromBody(await readBody(request)) : runTrustProgramQualification(); return { ecosystem_maturity: result.ecosystem_maturity }; }

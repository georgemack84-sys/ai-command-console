export const RECOVERY_EVIDENCE_ERRORS = {
  BLOCKED_UNSAFE_EVIDENCE_EXPORT: "BLOCKED_UNSAFE_EVIDENCE_EXPORT",
} as const;

export const RECOVERY_EVIDENCE_WARNINGS = {
  TIMELINE_STATE_MISMATCH: "Timeline does not explain current state",
  OPERATOR_ACTIONS_RESTRICTED: "Operator actions may be restricted",
} as const;

export const RECOVERY_EVIDENCE_VERSION = "3.5D-2" as const;

export const RECOVERY_EVIDENCE_HASH_ALGORITHM = "sha256" as const;


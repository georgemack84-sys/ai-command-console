# Search Model

Phase 5 search is deterministic and local. It indexes note titles, Markdown body, notebook names, tag names, and attachment filenames.

Search supports keyword, phrase, prefix-style token matching, basic Boolean FTS queries, notebook filters, tag filters, and archived-note exclusion by default. Attachment filename matches are returned as attachment results.

Semantic search and AI retrieval are intentionally deferred.

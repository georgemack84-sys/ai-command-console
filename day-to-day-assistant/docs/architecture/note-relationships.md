# Note Relationships

Note relationships are explicit rows in `note_links`. Supported targets are `NOTE`, `TASK`, `EVENT`, `FOLLOW_UP`, `REMINDER`, and `PROJECT`.

Supported relationship types are `REFERENCE`, `BACKGROUND`, `MEETING_NOTES`, `PREPARATION`, `OUTCOME`, and `RELATED`.

Backlinks are derived from inbound `NOTE` links and are not separately stored.

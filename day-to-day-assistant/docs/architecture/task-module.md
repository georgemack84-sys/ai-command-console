# Task Module

The task module owns task lists, lightweight projects, tasks, recurrence rules, task history, task activity, and task audit events.

The module enforces explicit lifecycle transitions and optimistic concurrency through the `version` field when clients provide it.

The module has no AI provider dependency.

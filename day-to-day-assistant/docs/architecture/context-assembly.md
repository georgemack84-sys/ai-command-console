# Context Assembly

Context assembly retrieves only the local records needed for the classified intent. It avoids archived data by default and caps retrieved records to keep context concise.

Context packages store sources, cited records, retrieval reasons, and generation time. The package is derived from local modules and can be inspected independently from the final response.

Context optimization removes duplicate citations and favors explicit request terms, current dates, recent records, and requested domains.

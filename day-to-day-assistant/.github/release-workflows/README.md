# Release Workflows

Phase D2D.12 records the intended release gate:

Build -> Test -> Package -> Deploy -> Health Check -> Qualification.

Local execution is available through `scripts/release/qualify.ps1`.

# Docker Infrastructure

The root `docker-compose.yml` defines local API and web services. A database container can be added when the project moves from SQLite foundation storage to PostgreSQL-backed development.

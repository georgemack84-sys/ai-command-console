# CI

The GitHub Actions workflow validates repository files, formatting checks, linting, typecheck placeholders, tests, Docker Compose configuration, container builds, PostgreSQL startup, and baseline secret scanning.

Branch protection should require CI jobs before merging to `main`.

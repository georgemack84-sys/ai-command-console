$ErrorActionPreference = "Stop"
Set-Location (Resolve-Path "$PSScriptRoot/../..")
Write-Host "Integration test placeholder passed. Full PostgreSQL integration tests require Docker daemon availability."

# Phase 26 — Self-Directed Learning Planner Exit Validation

Validated on 2026-09-03:

- `npx tsc --noEmit --pretty false`
- `npx vitest run --config vitest.config.mjs --environment node --pool forks --maxWorkers 1 --no-file-parallelism tests/unit/learning-constitution/phase26Acceptance.test.ts`

The acceptance scenario derives a recurring knowledge opportunity from prediction corrections, creates a scoped proposal, and proves the proposal cannot execute before explicit human approval issues a matching lease. It retains baseline and independent post-learning evidence, records an improved outcome, and proves the outcome cannot itself resolve the original gap.

import { describe, expect, it } from "vitest";
import { StrategyOverrideService } from "@/services/learning-constitution";
import type { StrategyDefinition, StrategySelectionRecord } from "@/types/learning-constitution";
const at = "2026-09-05T01:00:00.000Z"; const human = { actorId: "human:teacher", actorType: "HUMAN" as const };
const selection: StrategySelectionRecord = { selectionId: "SEL", requestId: "REQ", objectiveProfileId: "OBJ", selectedStrategyId: "S-RECOMMENDED", mode: "EXPLOIT", status: "RECOMMENDED", scores: [], rationale: [], policyVersion: "p", classifierVersion: "c", registryVersion: "r", evidenceSnapshotId: "e", createdAt: at, immutable: true, recommendationOnly: true, executionPermissionGranted: false, authorityEffect: "UNCHANGED" };
const strategy: StrategyDefinition = { strategyId: "S-ALTERNATE", name: "Alternate", version: 1, lifecycle: "ACTIVE", purpose: ["PRACTICE"], applicableContexts: ["REASONING"], prerequisites: [], steps: [{ ordinal: 1, primitive: "EXPLAIN" }], parameters: [], stopConditions: ["MAX_ATTEMPTS"], parentVersionId: null, evidenceIds: [], knownExceptions: [], createdBy: human, createdAt: at, learningEffect: "NONE", authorityEffect: "UNCHANGED", executionPermissionGranted: false };
describe("Phase 40 strategy override", () => { it("records a human alternate without execution authority", () => { expect(new StrategyOverrideService().create({ overrideId: "OVR", selection, alternate: strategy, reason: "Demonstration first", expectedOutcome: "Faster early comprehension", actor: human, createdAt: at })).toMatchObject({ selectedStrategyId: "S-ALTERNATE", overriddenStrategyId: "S-RECOMMENDED", requiresApprovedPlan: true, executionPermissionGranted: false }); }); it("rejects agent overrides and inactive alternatives", () => { const service = new StrategyOverrideService(); expect(() => service.create({ overrideId: "bad", selection, alternate: strategy, reason: "x", expectedOutcome: "y", actor: { actorId: "agent", actorType: "AGENT" }, createdAt: at })).toThrow("Only a human"); expect(() => service.create({ overrideId: "bad2", selection, alternate: { ...strategy, lifecycle: "SUSPENDED" }, reason: "x", expectedOutcome: "y", actor: human, createdAt: at })).toThrow("active alternate"); }); });

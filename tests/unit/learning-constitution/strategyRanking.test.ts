import { describe, expect, it } from "vitest";
import { StrategyRankingService } from "@/services/learning-constitution";
import type { StrategyPerformanceProfile, StrategySelectionRequest } from "@/types/learning-constitution";
const at = "2026-09-04T08:00:00.000Z"; const request: StrategySelectionRequest = { requestId: "SEL-RANK-38", objectiveId: "LO-38", context: { contextId: "CTX", objectiveId: "LO-38", domain: "Architecture", problems: ["COMPLEX_SKILL"], strategyContexts: ["REASONING"], horizon: "LONG_TERM", requiredOutcomes: { retention: "HIGH", generalization: "HIGH", boundaryRecognition: "HIGH" }, risk: "MEDIUM", createdAt: at, immutable: true }, learner: { snapshotId: "LS", objectiveId: "LO-38", dimensions: { CONCEPTUAL: "KNOWN", APPLICATION: "PARTIALLY_KNOWN", GENERALIZATION: "WEAK", BOUNDARY_RECOGNITION: "WEAK", RETENTION: "UNKNOWN", CALIBRATION: "MODERATE" }, satisfiedPrerequisites: [], uncertainPrerequisites: [], failedPrerequisites: [], sourceIds: [], createdAt: at, immutable: true }, budget: { timeMinutes: 45, tokenBudget: 1000, teacherAvailability: "LOW", risk: "MEDIUM", requiredRetention: "HIGH" }, requiredResources: [], availableResources: [], hardConstraints: ["GOVERNANCE"], createdAt: at, recommendationOnly: true, executionPermissionGranted: false, authorityEffect: "UNCHANGED" };
const profile = (id: string, sessions: number, retention: number, tokens: number): StrategyPerformanceProfile => ({ profileId: `P-${id}`, strategyId: id, context: "REASONING", sampleSize: sessions, meanImmediateGain: 20, meanRetention: retention, meanGeneralization: retention, meanBoundaryRecognition: retention, meanAdversarialPerformance: retention, meanCorrections: 1, meanHumanInterventions: 1, meanTokenCost: tokens, evidenceState: "SUPPORTED_PATTERN", sourceRunIds: [], interpretation: "OBSERVED_ASSOCIATION", causalClaim: false, createdAt: at, projectionOnly: true, authorityEffect: "UNCHANGED", executionPermissionGranted: false });
describe("Phase 38 contextual ranking", () => { it("penalizes weak distant evidence and exposes Pareto tradeoffs without causal claims", () => { const ranking = new StrategyRankingService().rank({ rankingId: "RANK-38", request, candidateStrategyIds: ["S-ROBUST", "S-CHEAP", "S-WEAK"], profiles: [profile("S-ROBUST", 12, 90, 800), profile("S-CHEAP", 12, 80, 200), profile("S-WEAK", 2, 95, 100)], createdAt: at }); expect(ranking.entries.find((entry) => entry.strategyId === "S-WEAK")?.utility.uncertaintyPenalty).toBe(50); expect(ranking.entries.filter((entry) => entry.paretoOptimal).map((entry) => entry.strategyId)).toEqual(expect.arrayContaining(["S-ROBUST", "S-CHEAP"])); expect(ranking).toMatchObject({ recommendationOnly: true, executionPermissionGranted: false, authorityEffect: "UNCHANGED" }); }); });

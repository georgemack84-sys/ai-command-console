import { describe, expect, it } from "vitest";
import { ReflectionRemediationPlanner } from "@/services/learning-constitution";
import type { ReflectionCase, ReflectionPrerequisiteDiagnosis, ReflectionSelectedCause } from "@/types/learning-constitution";
const actor = { actorId: "human:teacher", actorType: "HUMAN" as const }; const stamp = "2026-09-02T09:00:00.000Z";
const reflection: ReflectionCase = { reflectionId: "RF-1", trigger: "EVALUATION_FAILURE", evaluationId: "EVAL-1", skillId: "SK-ROADMAP", expectedOutcome: "Order dependencies.", observedOutcome: "Missed dependency.", failureType: "PREREQUISITE_GAP", failureLocation: "RETRIEVE_KNOWLEDGE", relevantKnowledgeIds: [], relevantPrincipleIds: [], relevantProcedureIds: [], relevantExampleIds: [], relevantExceptionIds: [], prerequisiteSkillIds: ["SK-DEPENDENCY"], status: "CAUSE_IDENTIFIED", createdBy: actor, createdAt: stamp, resolvedAt: null, masteryEffect: "NONE", durableKnowledgeEffect: "NONE", executionPermissionGranted: false };
const selected: ReflectionSelectedCause = { reflectionId: reflection.reflectionId, causeId: "RC-1", failureType: reflection.failureType, confidence: 0.8, selectionReason: "Graph and evaluation evidence agree.", selectedBy: actor, selectedAt: stamp, status: "SELECTED" };
const prerequisite: ReflectionPrerequisiteDiagnosis = { reflectionId: reflection.reflectionId, status: "RECOMMENDED", targetSkillId: "SK-DEPENDENCY", dependencyId: "SD-1", reason: "Mastery is below threshold.", evidenceIds: ["EE-1"] };
describe("Phase 22 minimal remediation planning", () => { it("targets only the weak prerequisite and requires bounded focused practice", () => { expect(new ReflectionRemediationPlanner().plan({ planId: "RP-1", reflection, selected, prerequisite, createdAt: stamp })).toMatchObject({ intervention: "PREREQUISITE_PRACTICE", targetSkillId: "SK-DEPENDENCY", practiceTransferLevels: ["EXACT", "MODIFIED"], reevaluationStrategy: "ORIGINAL_EVALUATION", maximumAttempts: 3, masteryEffect: "NONE" }); }); it("rejects prerequisite remediation without graph evidence", () => { expect(() => new ReflectionRemediationPlanner().plan({ planId: "RP-2", reflection, selected, prerequisite: { ...prerequisite, status: "INSUFFICIENT_EVIDENCE", targetSkillId: null }, createdAt: stamp })).toThrow("evidenced graph recommendation"); }); });

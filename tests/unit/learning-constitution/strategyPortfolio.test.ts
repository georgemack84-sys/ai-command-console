import { describe, expect, it } from "vitest";
import { StrategyPortfolioService } from "@/services/learning-constitution";
const at = "2026-09-04T10:00:00.000Z";
describe("Phase 38 portfolios and exploration", () => { it("requires justified stages and approved-plan handoff", () => { const portfolio = new StrategyPortfolioService().portfolio({ portfolioId: "PORT-38", objectiveId: "LO-38", stages: [{ ordinal: 1, strategyId: "S-ACQUIRE", transitionGate: "MINIMUM_ASSESSMENT", rationale: "Establish concepts." }, { ordinal: 2, strategyId: "S-TRANSFER", transitionGate: "NOVEL_APPLICATION", rationale: "Test transfer." }], createdAt: at }); expect(portfolio).toMatchObject({ status: "PROPOSED", requiresApprovedPlan: true, executionPermissionGranted: false, authorityEffect: "UNCHANGED" }); }); it("explores only inside a low-risk, rollback-required budget", () => { const service = new StrategyPortfolioService(); const budget = { maximumExperimentalRuns: 2, experimentalRunsUsed: 0, maximumExperimentalTokenShare: .1, experimentalTokenShareUsed: .02, maximumExperimentalTimeShare: .15, experimentalTimeShareUsed: .02, allowedRisk: "LOW" as const, rollbackRequired: true as const }; expect(service.explore({ decisionId: "EXPLORE-38", risk: "LOW", budget, validatedStrategyId: "S-VALID", experimentalStrategyId: "S-EXPERIMENT", experimentalEvidenceState: "EARLY_SIGNAL", createdAt: at })).toMatchObject({ mode: "EXPLORE", selectedStrategyId: "S-EXPERIMENT", executionPermissionGranted: false }); expect(service.explore({ decisionId: "EXPLOIT-38", risk: "HIGH", budget, validatedStrategyId: "S-VALID", experimentalStrategyId: "S-EXPERIMENT", experimentalEvidenceState: "EARLY_SIGNAL", createdAt: at })).toMatchObject({ mode: "EXPLOIT", selectedStrategyId: "S-VALID" }); }); });

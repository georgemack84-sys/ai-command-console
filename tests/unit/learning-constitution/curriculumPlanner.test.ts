import { describe, expect, it } from "vitest";
import { CurriculumPlannerService } from "@/services/learning-constitution";
const at = "2026-09-03T09:00:00.000Z"; const lease = { leaseId: "L-27", proposalId: "LP-27", approvalId: "HA-27", scope: "SYSTEM_DESIGN", method: "SOCRATIC_INTERVIEW" as const, maximumQuestions: 5, issuedBy: { actorId: "human:teacher", actorType: "HUMAN" as const }, issuedAt: at, expiresAt: "2026-09-04T09:00:00.000Z", status: "ACTIVE" as const };
describe("Phase 27 curriculum planner", () => { it("skips demonstrated prerequisites and orders remediation before dependent teaching", () => { const curriculum = new CurriculumPlannerService().plan({ curriculumId: "CUR-1", goal: "Architecture roadmap planning", lease, createdAt: at, skills: [{ skillId: "A", prerequisites: [], mastery: "DEMONSTRATED", epistemicState: "KNOWN", evidenceIds: ["EV-A"] }, { skillId: "B", prerequisites: ["A"], mastery: "WEAK", epistemicState: "PARTIALLY_KNOWN", evidenceIds: ["EV-B"] }, { skillId: "C", prerequisites: ["B"], mastery: "UNKNOWN", epistemicState: "UNKNOWN", evidenceIds: [] }] }); expect(curriculum.units.map((unit) => [unit.skillId, unit.disposition])).toEqual([["A", "SKIP"], ["B", "REMEDIATE"], ["C", "TEACH"]]); }); it("halts rather than inventing an order for cyclic dependencies", () => { expect(() => new CurriculumPlannerService().plan({ curriculumId: "CUR-cycle", goal: "Goal", lease, createdAt: at, skills: [{ skillId: "A", prerequisites: ["B"], mastery: "UNKNOWN", epistemicState: "UNKNOWN", evidenceIds: [] }, { skillId: "B", prerequisites: ["A"], mastery: "UNKNOWN", epistemicState: "UNKNOWN", evidenceIds: [] }] })).toThrow("dependency cycle"); }); });

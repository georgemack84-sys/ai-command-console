import { describe, expect, it } from "vitest";
import { StrategyEvaluationAnalysisService } from "@/services/learning-constitution";
import type { StrategyEvaluation, StrategyEvaluationProfile } from "@/types/learning-constitution";
const at = "2026-09-04T14:00:00.000Z"; const actor = { actorId: "agent:evaluator", actorType: "AGENT" as const };
const evaluation = (): StrategyEvaluation => ({ evaluationId: "E-39", strategyId: "S-39", strategyVersion: 1, objectiveId: "LO", executionId: "RUN", contextId: "CTX", learnerStateSnapshotId: "L", difficulty: "HIGH", risk: "MEDIUM", prerequisiteState: "SATISFIED", startedAt: at, completedAt: at, metrics: [{ metric: "RETENTION_ACCURACY", value: 40, unit: "PERCENT", evidenceIds: ["R"], availability: "OBSERVED", observedAt: at, immutable: true }], competencyAchieved: false, failureReasons: ["Retention collapse."], practiceEvidenceIds: [], evaluationEvidenceIds: [], adversarialEvidenceIds: [], retentionEvidenceIds: ["R"], correctionEvidenceIds: [], status: "COMPLETE", createdBy: actor, createdAt: at, immutable: true, strategyMutationAuthorized: false, executionPermissionGranted: false, authorityEffect: "UNCHANGED" });
const profile = (id: string, values: Record<string, number>): StrategyEvaluationProfile => ({ profileId: `P-${id}`, strategyId: id, cohortId: "C-39", evaluationIds: [id], sampleSize: 10, metrics: values, confidence: "SUPPORTED", confidenceReasons: [], interpretation: "OBSERVED_ASSOCIATION", causalClaim: false, createdAt: at, projectionOnly: true, strategyMutationAuthorized: false, executionPermissionGranted: false, authorityEffect: "UNCHANGED" });
describe("Phase 39 failure attribution and dominance", () => { it("attributes a retention collapse as strategy evidence requiring reflection, not mutation", () => { expect(new StrategyEvaluationAnalysisService().attribute({ attributionId: "A-39", evaluation: evaluation(), selectionPrerequisitesFailed: false, evaluationEvidenceValid: true, createdAt: at })).toMatchObject({ classification: "STRATEGY_FAILURE", requiresReflection: true, requiresGovernedReview: true, strategyMutationAuthorized: false }); }); it("identifies dominated profiles while retaining contextual specialization", () => { const analysis = new StrategyEvaluationAnalysisService().dominance({ analysisId: "D-39", cohortId: "C-39", profiles: [profile("S-BETTER", { IMMEDIATE_ACCURACY: 90, NOVEL_ACCURACY: 90, RETENTION_ACCURACY: 90, TOKEN_COST: 100 }), profile("S-WORSE", { IMMEDIATE_ACCURACY: 80, NOVEL_ACCURACY: 80, RETENTION_ACCURACY: 80, TOKEN_COST: 200 })], createdAt: at }); expect(analysis.entries.find((entry) => entry.strategyId === "S-WORSE")).toMatchObject({ dominatedByStrategyIds: ["S-BETTER"], paretoEfficient: false }); expect(analysis).toMatchObject({ projectionOnly: true, strategyMutationAuthorized: false, executionPermissionGranted: false }); }); });

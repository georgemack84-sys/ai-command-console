import { describe, expect, it } from "vitest";
import { StrategyEvaluationProfileService } from "@/services/learning-constitution";
import type { StrategyComparisonCohort, StrategyEvaluation } from "@/types/learning-constitution";
const at = "2026-09-04T13:00:00.000Z"; const actor = { actorId: "agent:evaluator", actorType: "AGENT" as const };
const cohort: StrategyComparisonCohort = { cohortId: "COHORT-39", domain: "Architecture", difficulty: "HIGH", risk: "MEDIUM", prerequisiteState: "SATISFIED", evaluationType: "NATURAL", createdAt: at, immutable: true };
const evaluation = (id: string, risk: StrategyEvaluation["risk"] = "MEDIUM"): StrategyEvaluation => ({ evaluationId: id, strategyId: "S-39", strategyVersion: 1, objectiveId: "LO", executionId: `RUN-${id}`, contextId: "CTX", learnerStateSnapshotId: "LEARNER", difficulty: "HIGH", risk, prerequisiteState: "SATISFIED", startedAt: at, completedAt: at, metrics: [{ metric: "NOVEL_ACCURACY", value: 80, unit: "PERCENT", evidenceIds: [id], availability: "OBSERVED", observedAt: at, immutable: true }, { metric: "RETENTION_ACCURACY", value: 70, unit: "PERCENT", evidenceIds: [id], availability: "OBSERVED", observedAt: at, immutable: true }], competencyAchieved: true, failureReasons: [], practiceEvidenceIds: [], evaluationEvidenceIds: [id], adversarialEvidenceIds: [], retentionEvidenceIds: [id], correctionEvidenceIds: [], status: "COMPLETE", createdBy: actor, createdAt: at, immutable: true, strategyMutationAuthorized: false, executionPermissionGranted: false, authorityEffect: "UNCHANGED" });
describe("Phase 39 evaluation cohorts and confidence", () => { it("compares only like-for-like non-invalid evidence and preserves sparse-evidence uncertainty", () => { const profile = new StrategyEvaluationProfileService().profile({ profileId: "PROFILE-39", strategyId: "S-39", cohort, evaluations: [evaluation("E1"), evaluation("E2"), evaluation("OUT", "HIGH")], createdAt: at }); expect(profile).toMatchObject({ sampleSize: 2, metrics: { NOVEL_ACCURACY: 80, RETENTION_ACCURACY: 70 }, confidence: "EXPERIMENTAL", interpretation: "OBSERVED_ASSOCIATION", causalClaim: false, strategyMutationAuthorized: false, executionPermissionGranted: false }); expect(profile.confidenceReasons.join(" ")).toContain("Insufficient evidence"); }); });

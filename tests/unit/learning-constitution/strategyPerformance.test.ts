import { describe, expect, it } from "vitest";
import { StrategyPerformanceService } from "@/services/learning-constitution";
import type { StrategyPerformanceMeasurement } from "@/types/learning-constitution";
const at = "2026-09-04T03:00:00.000Z"; const measure = (id: string): StrategyPerformanceMeasurement => ({ runId: id, strategyId: "S-37", context: "REASONING", immediateGain: 30, retention: 40, generalization: 45, boundaryRecognition: 80, adversarialPerformance: 60, correctionCount: 2, humanInterventionCount: 1, tokenCost: 500, timeMinutes: 30, valid: true, observedAt: at });
describe("Phase 37 strategy performance", () => { it("produces context-aware observational profiles, not causal winner claims", () => { const profile = new StrategyPerformanceService().profile({ profileId: "PROFILE-37", strategyId: "S-37", context: "REASONING", measurements: [measure("R1"), measure("R2"), measure("R3")], createdAt: at }); expect(profile).toMatchObject({ sampleSize: 3, meanImmediateGain: 30, meanRetention: 40, evidenceState: "EARLY_SIGNAL", interpretation: "OBSERVED_ASSOCIATION", causalClaim: false, projectionOnly: true, authorityEffect: "UNCHANGED" }); }); it("detects failure and offers only an approval-gated remediation", () => { const service = new StrategyPerformanceService(); const failure = service.detect({ failureId: "FAIL-37", measurement: measure("R1"), evidenceIds: ["SNAP-37"], createdAt: at })!; expect(failure).toMatchObject({ kind: "FALSE_MASTERY", status: "REQUIRES_REVIEW", executionPermissionGranted: false }); expect(service.remediate({ recommendationId: "REMED-37", failure, createdAt: at })).toMatchObject({ recommendedPrimitive: "ADVERSARIAL_TEST", status: "PROPOSED", requiresApprovedPlan: true, executionPermissionGranted: false }); }); });

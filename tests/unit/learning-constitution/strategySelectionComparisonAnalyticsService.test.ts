import { describe, expect, it } from "vitest";
import { StrategySelectionComparisonAnalyticsService } from "@/services/learning-constitution";
import type { LearningObjectiveProfile, SelectionOutcome, StrategyOverride, StrategySelectionRecord } from "@/types/learning-constitution";
const at = "2026-09-05T02:00:00.000Z"; const actor = { actorId: "human:teacher", actorType: "HUMAN" as const };
const profile: LearningObjectiveProfile = { profileId: "OBJ", objectiveId: "LO", domain: "Security", primaryType: "DIAGNOSTIC", typeConfidence: .9, secondaryTypes: [], currentMastery: "COMPETENT", targetMastery: "ADVANCED", risk: "HIGH", transferRequirement: "HIGH", retentionRequirement: "HIGH", prerequisites: [], knowledgeGapIds: [], constraints: [], classifierVersion: "v1", createdAt: at, immutable: true };
const selection: StrategySelectionRecord = { selectionId: "SEL", requestId: "REQ", objectiveProfileId: "OBJ", selectedStrategyId: "SYSTEM", mode: "EXPLOIT", status: "RECOMMENDED", scores: [], rationale: [], policyVersion: "p", classifierVersion: "c", registryVersion: "r", evidenceSnapshotId: "e", createdAt: at, immutable: true, recommendationOnly: true, executionPermissionGranted: false, authorityEffect: "UNCHANGED" };
const outcome = (id: string, strategyId: string, overrideId: string | null, value: number): SelectionOutcome => ({ outcomeId: id, selectionId: "SEL", strategyId, overrideId, curriculumId: "CUR", evaluationId: `E-${id}`, executionId: `RUN-${id}`, metrics: [{ metric: "NOVEL_ACCURACY", value, unit: "PERCENT", evidenceIds: [id], availability: "OBSERVED", observedAt: at, immutable: true }], immediateEvidenceIds: [id], novelApplicationEvidenceIds: [id], retentionEvidenceIds: [id], calibrationEvidenceIds: [id], invalidEvidenceIds: [], status: "COMPLETE", createdAt: at, immutable: true, recommendationOnly: true, executionPermissionGranted: false, authorityEffect: "UNCHANGED" });
const override: StrategyOverride = { overrideId: "OVR", selectionId: "SEL", selectedStrategyId: "HUMAN", overriddenStrategyId: "SYSTEM", reason: "Reason", expectedOutcome: "Outcome", actor, createdAt: at, immutable: true, requiresApprovedPlan: true, executionPermissionGranted: false, authorityEffect: "UNCHANGED" };
describe("Phase 40 comparison analytics", () => { it("compares complete valid outcomes without causal claims", () => { const result = new StrategySelectionComparisonAnalyticsService().compare({ profile, selection, overrides: [override], outcomes: [outcome("S1", "SYSTEM", null, 70), outcome("H1", "HUMAN", "OVR", 90)] }); expect(result).toMatchObject({ system: { sampleSize: 1 }, humanOverride: { sampleSize: 1 }, confidence: "INSUFFICIENT", interpretation: "OBSERVED_ASSOCIATION", causalClaim: false }); expect(result.humanOverride.metrics.NOVEL_ACCURACY).toBe(90); }); });

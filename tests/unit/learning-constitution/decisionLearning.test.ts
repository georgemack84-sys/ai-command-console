import { describe, expect, it } from "vitest"; import { ConservativeDecisionValidator, DecisionCandidateService } from "@/services/learning-constitution"; import type { DecisionCandidate } from "@/types/learning-constitution";
const now = "2026-09-01T00:00:00.000Z"; const decision = (overrides: Partial<DecisionCandidate> = {}): DecisionCandidate => ({ decisionId: "decision:1", problem: "Choose a local database.", context: "Prototype requires failure isolation.", scope: [{ type: "PROJECT", id: "noesis" }], constraints: [{ constraintId: "c:1", description: "Isolate failures.", kind: "HARD", provenanceId: "teaching:1" }], assumptions: [{ assumptionId: "a:1", statement: "Prototype remains local.", challengeable: true, provenanceId: "teaching:1" }], options: [{ optionId: "postgres", description: "PostgreSQL", disposition: "REJECTED", rejectionReason: "Too heavy for prototype." }, { optionId: "sqlite", description: "SQLite", disposition: "SELECTED" }], rationale: "SQLite provides failure isolation for this prototype.", tradeoffs: ["Less production parity."], principleIds: [], preferenceIds: ["pref:postgres"], provenanceIds: ["teaching:1"], authority: "HUMAN_DECISION", status: "CANDIDATE", significant: true, universalClaim: false, createdBy: { actorId: "human:george", actorType: "HUMAN" }, createdAt: now, immutable: true, executionPermissionGranted: false, ...overrides });
describe("Phase 17 decision boundary", () => { it("preserves selected and rejected options as contextual evidence", () => expect(new ConservativeDecisionValidator().validate(decision())).toMatchObject({ status: "VALID", executionPermissionGranted: false })); it("defers casual choices and rejects universalized decisions", () => { const validator = new ConservativeDecisionValidator(); expect(validator.validate(decision({ significant: false }))).toMatchObject({ status: "DEFER", reasonCodes: ["DECISION_NOT_SIGNIFICANT"] }); expect(validator.validate(decision({ universalClaim: true }))).toMatchObject({ status: "REJECT", reasonCodes: expect.arrayContaining(["DECISION_IS_NOT_UNIVERSAL_RULE"]) }); }); });

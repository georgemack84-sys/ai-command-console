import type { KnowledgeScopeReference } from "./knowledgeScope";
import type { ProvenanceActor } from "./provenance";

export const PRINCIPLE_EVIDENCE_TYPES = ["DECISION", "CORRECTION", "INSTRUCTION", "EXCEPTION", "PREFERENCE", "APPROVED_EXAMPLE", "REJECTED_EXAMPLE", "PRIOR_PRINCIPLE", "TEACH_BACK_RESULT", "CONFLICT_RESOLUTION"] as const;
export type PrincipleEvidenceType = (typeof PRINCIPLE_EVIDENCE_TYPES)[number];
export type PrincipleEvidence = Readonly<{ evidenceId: string; type: PrincipleEvidenceType; provenanceId: string; scope: KnowledgeScopeReference; diversityKey: string; observedAt: string; supportsPattern: boolean; immutable: true }>;
export const POTENTIAL_PATTERN_STATUSES = ["PATTERN_ONLY", "RETAINED", "REJECTED", "REASSESSMENT_REQUIRED"] as const;
export type PotentialPatternStatus = (typeof POTENTIAL_PATTERN_STATUSES)[number];
/** A pattern is an evidence-backed hypothesis. It carries no authority and cannot become durable knowledge. */
export type PotentialPattern = Readonly<{ patternId: string; description: string; supportingEvidence: readonly PrincipleEvidence[]; contradictingEvidence: readonly PrincipleEvidence[]; relevantCorrectionIds: readonly string[]; relevantExceptionIds: readonly string[]; observedScope: KnowledgeScopeReference; observationCount: number; diversityCount: number; confidence: number; proposedExplanation: string; alternativeExplanations: readonly string[]; disconfirmationCondition: string; status: PotentialPatternStatus; createdBy: ProvenanceActor; createdAt: string; immutable: true }>;
export type PatternEvaluation = Readonly<{ patternId: string; disposition: "RETAIN" | "REJECT" | "REASSESS"; reasons: readonly string[]; persistenceEffect: "NONE"; authorityEffect: "UNCHANGED"; executionPermissionGranted: false; candidatePrincipleCreated: false }>;
export type PatternDetectionInput = Readonly<{ patternId: string; description: string; supportingEvidence: readonly PrincipleEvidence[]; contradictingEvidence?: readonly PrincipleEvidence[]; relevantCorrectionIds?: readonly string[]; relevantExceptionIds?: readonly string[]; observedScope: KnowledgeScopeReference; proposedExplanation: string; alternativeExplanations: readonly string[]; disconfirmationCondition: string; createdBy: ProvenanceActor; createdAt: string }>;
export interface PatternEvaluator { evaluate(pattern: PotentialPattern): PatternEvaluation; }
export const PRINCIPLE_GENERALIZATION_LEVELS = [0, 1, 2, 3, 4] as const;
export type PrincipleGeneralizationLevel = (typeof PRINCIPLE_GENERALIZATION_LEVELS)[number];
/** An agent hypothesis. Confidence and evidence cannot change this authority value. */
export type CandidatePrinciple = Readonly<{ candidatePrincipleId: string; patternId: string; proposedPrinciple: string; rationale: string; evidenceIds: readonly string[]; counterevidenceIds: readonly string[]; scope: KnowledgeScopeReference; preconditions: readonly string[]; exceptions: readonly string[]; confidence: number; authority: "AGENT_INFERRED"; generalizationLevel: PrincipleGeneralizationLevel; alternativeInterpretations: readonly string[]; disconfirmationCondition: string; provenanceIds: readonly string[]; reviewStatus: "AWAITING_HUMAN_REVIEW"; createdBy: ProvenanceActor; createdAt: string; reviewExpiresAt: string; immutable: true }>;
export type CandidatePrincipleExtractionInput = Readonly<{ candidatePrincipleId: string; pattern: PotentialPattern; evaluation: PatternEvaluation; proposedPrinciple: string; rationale: string; scope: KnowledgeScopeReference; preconditions: readonly string[]; exceptions: readonly string[]; generalizationLevel: PrincipleGeneralizationLevel; createdBy: ProvenanceActor; createdAt: string; reviewExpiresAt: string }>;
export type CandidatePrincipleEligibility = Readonly<{ candidatePrincipleId: string; status: "ELIGIBLE" | "EXPIRED"; reason: "AWAITING_HUMAN_REVIEW" | "REVIEW_WINDOW_EXPIRED"; persistenceEffect: "NONE"; authorityEffect: "UNCHANGED"; executionPermissionGranted: false }>;
export const PRINCIPLE_REVIEW_ACTIONS = ["APPROVE", "MODIFY", "REJECT", "NARROW_SCOPE", "EXPAND_SCOPE", "ADD_EXCEPTION", "REQUEST_MORE_EVIDENCE", "MERGE_WITH_EXISTING"] as const;
export type PrincipleReviewAction = (typeof PRINCIPLE_REVIEW_ACTIONS)[number];
/** Immutable human decision over a candidate; it preserves, rather than rewrites, agent inference. */
export type CandidatePrincipleReview = Readonly<{ reviewId: string; candidatePrincipleId: string; action: PrincipleReviewAction; actor: ProvenanceActor; note: string; reviewedAt: string; immutable: true }>;
/** Human authorization is explicit but still cannot write to the durable registry without subsequent governance checks. */
export type HumanAuthorizedPrinciple = Readonly<{ interpretationId: string; candidatePrincipleId: string; reviewId: string; statement: string; rationale: string; scope: KnowledgeScopeReference; preconditions: readonly string[]; exceptions: readonly string[]; authority: "HUMAN_DECISION"; status: "PENDING_CONFLICT_AND_GATE"; authorizedBy: ProvenanceActor; authorizedAt: string; immutable: true; executionPermissionGranted: false }>;
export type PrincipleReviewRequest = Readonly<{ review: CandidatePrincipleReview; candidate: CandidatePrinciple; interpretation?: Omit<HumanAuthorizedPrinciple, "candidatePrincipleId" | "reviewId" | "authority" | "status" | "authorizedBy" | "authorizedAt" | "immutable" | "executionPermissionGranted"> }>;
export interface CandidatePrincipleReviewRepository { append(review: CandidatePrincipleReview): Promise<CandidatePrincipleReview>; appendInterpretation(interpretation: HumanAuthorizedPrinciple): Promise<HumanAuthorizedPrinciple>; list(candidatePrincipleId: string): Promise<readonly CandidatePrincipleReview[]>; }
export const DURABLE_PRINCIPLE_STATUSES = ["ACTIVE", "LIMITED", "SUPERSEDED", "REVOKED", "UNDER_REVIEW"] as const;
export type DurablePrincipleStatus = (typeof DURABLE_PRINCIPLE_STATUSES)[number];
export type DurablePrinciple = Readonly<{ principleId: string; durableKnowledgeId: string; interpretationId: string; statement: string; rationale: string; scope: KnowledgeScopeReference; preconditions: readonly string[]; exceptions: readonly string[]; authority: "HUMAN_DECISION"; status: DurablePrincipleStatus; provenanceIds: readonly string[]; createdAt: string; immutable: true }>;
export interface PrincipleRegistry { append(principle: DurablePrinciple): Promise<DurablePrinciple>; list(): Promise<readonly DurablePrinciple[]>; }
export type PrincipleApplicationRequest = Readonly<{ scope: KnowledgeScopeReference; contextFacts: readonly string[] }>;
export type PrincipleApplicationEvaluation = Readonly<{ principleId: string; status: "APPLIES" | "OUT_OF_SCOPE" | "PRECONDITIONS_UNMET" | "EXCEPTION_APPLIES" | "NOT_ACTIVE"; scopeMatched: boolean; satisfiedPreconditions: readonly string[]; unmetPreconditions: readonly string[]; applicableExceptions: readonly string[]; persistenceEffect: "NONE"; authorityEffect: "UNCHANGED"; executionPermissionGranted: false }>;
export type PrincipleReassessmentTrigger = Readonly<{ triggerId: string; principleId: string; sourceType: "CORRECTION" | "COUNTEREVIDENCE" | "CONFLICT"; sourceId: string; rationale: string; requestedAt: string; immutable: true }>;
export type PrincipleReassessmentResult = Readonly<{ trigger: PrincipleReassessmentTrigger; status: "REVIEW_REQUESTED"; persistenceEffect: "CREATED"; authorityEffect: "UNCHANGED"; executionPermissionGranted: false; principleModified: false }>;
export interface PotentialPatternRepository { append(pattern: PotentialPattern): Promise<PotentialPattern>; }
export interface CandidatePrincipleRepository { append(candidate: CandidatePrinciple): Promise<CandidatePrinciple>; }
export interface PrincipleReassessmentRepository { append(trigger: PrincipleReassessmentTrigger): Promise<PrincipleReassessmentTrigger>; }

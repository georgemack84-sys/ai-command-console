{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Scan Ports Input",
  "type": "object",
  "additionalProperties": false,
  "required": ["host"],
  "properties": {
    "host": { "type": "string", "enum": ["localhost"] }
  }
}
